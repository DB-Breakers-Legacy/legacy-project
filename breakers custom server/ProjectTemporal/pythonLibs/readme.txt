all modifications are tagged with #breaded

there is a much better way to do this without overwriting files but idrc atm, fix later

h2.py -

fixed DATA_END flag not being set on last data packet (wtf hypercorn)
